import { Literata, Montserrat, Playfair_Display } from "next/font/google";

export const literata = Playfair_Display({
  variable: "--font-literata",
  subsets: ["latin"],
});

export const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});
